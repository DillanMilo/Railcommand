# RailCommand store metadata — US English

Release version: `1.0.0`

The console-ready source of truth is `docs/mobile/store-listing.en-US.json`. Run
`npm run verify:store:metadata` before copying or saving any listing field. The check
enforces both stores' character limits, the US-only scope, approved URLs, matching
version/name/description values, and the claims prohibited below.

## Shared positioning

RailCommand is a private mobile field companion for authorized rail and construction
teams. It does not offer public signup, consumer purchases, or full offline project
administration.

## Apple

- Name (30): `RailCommand`
- Subtitle (30): `Offline rail field logs`
- Promotional text: `Capture daily field logs, photos, and optional location even when connectivity drops, then synchronize safely when service returns.`
- Keywords (100): `rail,construction,daily log,field report,project control,offline,safety,photos,crew`

Description:

> RailCommand gives authorized rail and construction teams a focused mobile workflow
> for daily field reporting.
>
> Use your organization-issued RailCommand account to select a project, review cached
> project and team information, create a daily log, attach field photos and optional
> location, and monitor synchronization status.
>
> Scoped offline support protects new daily-log drafts and photos when connectivity
> drops. Reconnect to synchronize each queued item safely. Existing records remain
> read-only offline, and administrative modules remain available in the connected web
> application.
>
> RailCommand is licensed directly to organizations. The mobile app contains no ads,
> in-app purchases, or public account registration.

What’s New for 1.0:

> Initial RailCommand mobile release with organization sign-in, project selection,
> cached field references, offline daily-log drafts, photo and optional location
> capture, durable synchronization, Sync Center, notifications, privacy controls, and
> account-deletion initiation.

## Google Play

- App name (30): `RailCommand`
- Short description (80): `Create rail field logs and photos, even when connectivity drops.`

Full description: use the Apple description above without the “What’s New” paragraph.

## Claims to avoid

Do not call v1 “full offline project management,” “real-time offline,” “guaranteed
background sync,” or “available worldwide.” Do not show deferred administration,
billing, EarthCam administration, RailBot voice, or offline existing-record editing in
screenshots or metadata. The native Cameras screen may show project-authorized
EarthCam share feeds, but marketing must not imply that video is available offline or
that EarthCam administration is included in the native release.
